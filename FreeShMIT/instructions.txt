# These are the instructions to run the MongoDB
# Updated 11/5/2017

# ---------------- Connect to Server ------------------------

npm
npm install express
npm express

docker run -p 27017:27017 --name FreeShMIT -d mongo
docker ps -a (should see only "FreeShMIT" docker)

npm package
npm install mongodb
dock
node index.js

docker-machine ip (ensure using this IP for both index.js and robo 3T

# ------------------ General Docker Commands ------------------

Build image

# the "-t" gives your image a name 
$ docker build -t YourImageName .

List Containers

# containers currently runing
$ docker ps -a

List Images

# list all docker images
$ docker images -a

$ docker start -a -i (number)

Delete containers

# Delete every Docker containers
# Must be run first because images are attached to containers
$ docker rm $(docker ps -a -q)

Delete images

# Delete every Docker image
$ docker rmi $(docker images -q)

Force delete images

# to force use
$ docker rmi -f $(docker images -q)

Stop container

$ docker stop <container id>
Shell in running container

# from docker 1.3 onwards
$ sudo docker exec -i -t <containerIdOrName> bash